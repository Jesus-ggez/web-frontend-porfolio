import { customElement, property } from "lit/decorators.js";
import { LitElement, html, css } from "lit";

@customElement('x-plan-description')
export class XPlanDescription extends LitElement {
    render() { return html`<div></div>`; }
}
